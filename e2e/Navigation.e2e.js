// import Navigation from '../src/navigations';

describe('Navigation', () => {
    beforeEach(async () => {
        await device.reloadReactNative();
    });

    // it('should go to Promotion', async () => {
    //     await expect(element(by.text('Promotions'))).toBeVisible();
    // });
    it('should go to Promotion', async () => {});
});
